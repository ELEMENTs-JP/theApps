@using theComponents.Add
@using theControls.Add
@using theControls.Connect
@using theControls.Layout
@using theControls.List
@using theControls.Selection
@using theControls.Tables
@inject ISqlDatabaseService sql

@if (RelatedItem != null && ItemType != null)
{
    <Frame Border="true" Rounded="true" CSS="@CSS">
        <Header>
            @if (ItemType != null)
            {
                <div class="d-flex flex-row align-items-center">

                    @if (!string.IsNullOrEmpty(Title))
                    {
                        <div class="fw-bold pe-2 me-2">@Title</div>
                    }
                    <ItemTypeSelector Items="@ItemTypes" OnClick="@( e => { ItemType = e; })" />
                </div>
            }
        </Header>
        <Related>
            <div class="hstack align-items-center">
                <div class="text-muted px-2">
                    @Association.ToSecureString()
                </div>
                <div class="vr mx-2"></div>
                <ConnectComp ItemType="@ItemType" RelatedItem="@RelatedItem" Association="@this.Association" />
                <div class="vr mx-2"></div>
                <AddArea ItemType="@ItemType" RelatedItem="@RelatedItem" Association="@this.Association" />
            </div>
        </Related>
        <Content>
            <ItemsList ItemType="@ItemType" RelatedItem="@RelatedItem" Association="@this.Association" />
        </Content>
        <Bottom>
            <AddFast ItemType="@ItemType" RelatedItem="@RelatedItem" Association="@this.Association" />
        </Bottom>
    </Frame>
}

@code {

    [Parameter]
    public string Title { get; set; } = string.Empty;

    [Parameter]
    public string CSS { get; set; } = string.Empty;

    IItemType ItemType { get; set; }  // Related ItemType // Parent ItemType 

    [Parameter]
    public IDTO RelatedItem { get; set; } // Related DTO 

    [Parameter]
    public List<IItemType> ItemTypes { get; set; } = new();

    [Parameter]
    public AssociationTyp Association { get; set; } = AssociationTyp.Association;


    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();

    }
    protected override async Task OnParametersSetAsync()
    {
        await base.OnParametersSetAsync();

        if (ItemTypes.Count() >= 1)
        {
            ItemType = ItemTypes.FirstOrDefault();
        }

    }
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await base.OnAfterRenderAsync(firstRender);

        if (firstRender)
        {
            // this.StateHasChanged();
        }
    }
}
