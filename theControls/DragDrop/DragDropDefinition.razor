
<div class="row g-0 flex-shrink-0 border rounded pointer hover p-2" 
     @onclick="@( async e => { await OnClick.InvokeAsync(Item); })">

@*     @if (Item["Icon"] != Icon.NULL.ToString())
    {
        <div class="col-auto pe-3">
             <SymbolIcon Symbol="@Item.Icon" />
         </div>
    } *@

    <div class="col pe-2">
        @Item.Title
     </div>
</div>

@code {

    [Parameter]
    public IDTO Item { get; set; }

    [Parameter]
    public EventCallback<IDTO> OnClick { get; set; }
}
